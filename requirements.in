# Прямые зависимости приложения. Точные пины — в requirements.txt.
# После правки этого файла обновите requirements.txt (pip freeze из рабочего venv).

# Веб-фреймворк и сервер
fastapi>=0.111,<0.112
uvicorn>=0.30,<0.31
python-multipart>=0.0.7,<0.1
email-validator>=2.0,<3

# Telegram
aiogram>=3.7,<4

# HTTP-клиент (Groq, embeddings, Resend, МойСклад)
httpx>=0.27,<0.28

# База данных
psycopg>=3.2,<4
psycopg-binary>=3.2,<4
psycopg-pool>=3.2,<4

# Redis (сессии и rate-limit в проде)
redis>=5.2,<6

# Авторизация дашборда
PyJWT>=2.8,<3
passlib[bcrypt]>=1.7,<2
bcrypt>=3.2,<4  # passlib 1.7.x несовместим с bcrypt 4+

# Email-уведомления
resend>=2.0,<3

# Наблюдаемость
sentry-sdk[fastapi]>=2.14,<3

# Конфигурация
python-dotenv>=1.0,<2

# Дев/CI
pytest>=9,<10
ruff>=0.15,<0.16
